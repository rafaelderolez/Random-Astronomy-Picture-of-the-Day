/*global App, Backbone*/

App.Models = App.Models || {};

(function() {
    'use strict';

    App.Models.PhotoModel = Backbone.Model.extend({
        defaults: {

        },

        initialize: function(options) {

        }
    });

})();
